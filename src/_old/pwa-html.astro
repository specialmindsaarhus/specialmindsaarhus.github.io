---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import VideoComponent from '../components/VdeoComponent.astro'
//import Engineering from './engineering.astro';

const d = new Date()
const whatDay = d.getDay()




---

<Layout title="Special Minds Aarhus @ Github">
	<main>
		<h1 class="mt-6">Vejlednig<span class="text-gradient">
			til HTML komponenterne
			</span></h1>
			<div class="instructions  !bg-black !border-4 p-8 rounded-lg " style="background:unset;">
				<h4 class="mt-4">
					Forberedende HTML øvelser
					<span class="text-sm ms-4">Hvis du vil mere i dybden med HTML</span>
				</h4>
				<ul class="list-none">

					<li>Box Modellen... 
						<span class="text-xs">
						Få papir fra Mark
						</span>
					</li>
					<li class="">
	
						<a href="https://www.canva.com/design/DAGBXMwvPJA/fxHo2fppB-I5B3_d7CFF_w/edit?utm_content=DAGBXMwvPJA&utm_campaign=designshare&utm_medium=link2&utm_source=sharebutton" class="no-underline">
							HTML Øvelser...
							<span class="text-xs">
								Følg link
								</span>
						</a>
					</li>
				</ul>
			</div>

			<section>
				<h2>Sådan indsætter du HTML og Komponenter i index.astro</h2>
<p>Når vi bygger vores PWA, hjælper komponenter os med at holde koden ren og overskuelig. Komponenter er små stykker kode, der fungerer som byggesten, og vi kan genbruge dem flere steder i vores app. I dette afsnit skal vi indsætte nogle komponenter i vores index.astro.</p>

<h3>Sådan gør du trin for trin:</h3>
<hr />

<h4>1. Importér komponenterne</h4>
<p>Først skal vi importere de komponenter, vi vil bruge. Det gør vi ved at tilføje følgende linjer øverst i din index.astro-fil. Disse linjer fortæller projektet, hvor komponenterne ligger, så vi kan bruge dem senere.</p>

<pre><code>
import Layout from &quot;../layouts/Layout.astro&quot;;
import ImageComponent from &quot;../components/ImageComponent.astro&quot;;
import IconWithText from &quot;../components/IconWithText.astro&quot;;
import ButtonComponent from &quot;../components/ButtonComponent.astro&quot;;
</code></pre>

<p>Nu har vi importeret tre komponenter:</p>
<ul>
  <li><strong>ImageComponent</strong>: Dette viser et billede af en kat.</li>
  <li><strong>IconWithText</strong>: Dette viser et ikon med noget tekst.</li>
  <li><strong>ButtonComponent</strong>: Dette er en knap, som vi kan klikke på for at få en ny kat.</li>
</ul>

<hr />

<h4>2. Indsæt ImageComponent</h4>
<p>Næste trin er at indsætte vores første komponent, <code>ImageComponent</code>, inde i &lt;div id=&quot;fetchContainer&quot;&gt;. Denne komponent viser et billede af en kat fra The Cat API. Du skal indsætte koden på det sted, hvor der står: &lt;!-- Indsæt ImageComponent --&gt;.</p>

<pre><code>
&lt;ImageComponent
  src=&quot;https://cdn2.thecatapi.com/images/ets_USqdE.jpg&quot;
  alt=&quot;Cat Image&quot; 
/&gt;
</code></pre>

<hr />

<h4>3. Indsæt tagline</h4>
<p>Efter kattebilledet skal vi vise en lille tagline, der beskriver, hvad appen gør. Denne tekst kan du indsætte under billedet som et <code>&lt;p&gt;</code>-element med klassen <code>text-center tiny-text</code>. Det skal placeres, hvor der står: &lt;!-- Indsæt tagline --&gt;.</p>

<pre><code>
&lt;p class=&quot;text-center tiny-text&quot;&gt;
  The CATalog &lt;br /&gt;
  Delivers Cats around the clock.
&lt;/p&gt;
</code></pre>

<hr />

<h4>4. Indsæt ikoner med tekst</h4>
<p>Nu vil vi vise nogle ikoner med tekst under taglinen. For at gøre det skal vi bruge komponenten <code>IconWithText</code>. Disse ikoner kan vise forskellig information om katten, som dens temperament, oprindelse og energiniveau.</p>
<p>Indsæt først en container til ikonerne med denne linje:</p>

<pre><code>
&lt;div class=&quot;text-container&quot;&gt;
</code></pre>

<p>Inde i denne container kan du nu tilføje flere instanser af <code>IconWithText</code>-komponenten, hvor hver instans har et ikon og noget tekst. Det skal indsættes, hvor der står: &lt;!-- Container til ikoner med tekst --&gt;.</p>

<pre><code>
&lt;IconWithText
  iconSrc=&quot;../../public/icons/pasport.svg&quot;
  subText=&quot;/Temperament&quot;
  id=&quot;fetchContentOne&quot; 
/&gt;
&lt;IconWithText
  iconSrc=&quot;../../public/icons/box.svg&quot;
  subText=&quot;/Origin&quot;
  id=&quot;fetchContentTwo&quot; 
/&gt;
&lt;IconWithText
  iconSrc=&quot;../../public/icons/energy.svg&quot;
  subText=&quot;/Energy level&quot;
  id=&quot;fetchContentThree&quot; 
/&gt;
</code></pre>

<hr />

<h4>5. Indsæt knappen</h4>
<p>Til sidst skal vi indsætte vores <code>ButtonComponent</code>. Denne knap viser en ny kat, når du klikker på den. Find stedet, hvor der står: &lt;!-- Indsæt ButtonComponent med attribute: text=&quot;Din tekst&quot; --&gt;, og indsæt denne linje kode:</p>

<pre><code>
&lt;ButtonComponent text=&quot;New Cat&quot; /&gt;
</code></pre>

			</section>
			<!--- 
            <VideoComponent 
            src="/video/github_vejledning.mp4"
			/>
			-->
			<br>
			<div style="border-bottom: 2px solid white;"></div>
			<br>


			
		
			
	</main>
</Layout>

<style>
	main {
		margin: auto;
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		color: white;
		font-size: 20px;
		line-height: 1.6;
	}
	.astro-a {
		position: absolute;
		top: -32px;
		left: 50%;
		transform: translatex(-50%);
		width: 220px;
		height: auto;
		z-index: -1;
	}
	h1 {
		font-size: 4rem;
		font-weight: 700;
		line-height: 1.125;
		text-align: center;
		margin-bottom: 1em;
	}

	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.instructions {
		margin-bottom: 2rem;
		border: 1px solid rgba(var(--accent-light), 25%);
		padding: 1.5rem;
		border-radius: 8px;
		background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
	}
	.instructions.color{

	}
	.instructions code {
		font-size: 0.8em;
		font-weight: bold;
		background: rgba(var(--accent-light), 12%);
		color: rgb(var(--accent-light));
		border-radius: 4px;
		padding: 0.3em 0.4em;
	}
	.instructions strong {
		color: rgb(var(--accent-light));
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(1fr, 1fr));
		gap: 2rem;
		padding: 0;
	}
</style>
